// In this we have to calculate the percentage of the discount


const listingPrice = 999;

const sellingPrice = 749;
const characterr = "this is string";

// var random;

const discountPercentage = ((listingPrice - sellingPrice)/listingPrice) * 100;

console.log(discountPercentage + "%");

const discountDisplayPercentage = (Math.round(discountPercentage ))

// const ran = Math.random(random);

console.log(discountDisplayPercentage + "% off ");

console.log(typeof(discountDisplayPercentage));
console.log(typeof(characterr));

// console.log(ran + "this is ");
